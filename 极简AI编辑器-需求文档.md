# 极简 AI 编辑器 · 需求文档（V1）

> **一句话定义**：一个打开任何文本文件不超过 0.5 秒、零学习成本、右侧带 AI 面板（总结/分析/改写/问答/翻译）的轻量编辑器。
>
> **产品哲学**：不是「功能少的 IDE」，而是「记事本的打开速度 + AI 的增强能力」。一切不为这条核心链路服务的功能，都不进 V1。

---

## 一、核心用户流程（V1 的全部体验）

```
启动软件
  └─ 空状态界面：三个入口 —— ① 打开文件（native 对话框）② 打开文件夹（native 对话框）③ 历史列表
       │
       ├─ 拖文件/文件夹进窗口 ────────┐
       └─ native 文件选择框 ─────────┤
                                   ▼
                          文件在 0.5s 内可编辑
                                   │
       ┌───────────────────────────┼───────────────────────────┐
       ▼                           ▼                           ▼
  正常编辑                 选中一段文字                 打开文件夹时
 （编辑/保存/查找替换）      点右侧 AI 按钮：              左侧显示文件树
                            总结 / 分析 / 改写            点击文件即打开为新标签
                            / 问答 / 翻译
                                   │
                                   ▼
                        改写/翻译结果直接替换原文
                        （Ctrl+Z 一步撤销）
                                   │
                          关闭软件 ──→ 状态清零
                                   │
                          再次启动 ──→ 回到空状态界面
                          （历史列表里能找到上次打开的文件）
```

---

## 二、功能需求

### 2.1 打开（最高优先级，产品的立身之本）

| 需求 | 说明 |
|------|------|
| F1 | native 文件选择框打开文件/文件夹（系统标准对话框，**不存在任何需要输入命令的路径**） |
| F2 | 拖入文件 → 直接打开编辑；拖入文件夹 → 打开并显示文件树 |
| F3 | **打开文件到可编辑 ≤ 0.5 秒**（常规文本文件，含软件冷启动后首次打开） |
| F4 | 历史列表：最近打开的文件和文件夹（建议 20 条，持久化），点击即开；可单条删除、可清空 |
| F5 | 简单多标签：可同时开多个文件，顶部标签切换，标签可关闭；**关闭软件后标签不保留** |
| F6 | 打开文件夹 → 左侧文件树，点击文件在新标签打开；树可收起 |

### 2.2 编辑（保留底线，不扩展）

| 需求 | 说明 |
|------|------|
| E1 | 文本编辑基础：输入/删除/剪贴板/多行选择与编辑/撤销重做 |
| E2 | 查找替换（当前文件内） |
| E3 | 编码处理：UTF-8 默认，自动检测并正确打开 GBK/GB18030 等中文编码，保存可选编码（中文用户刚需） |
| E4 | 换行符（LF/CRLF）识别与保留 |
| E5 | 自动换行开关；行列号显示 |
| E6 | CJK 字体开箱可用（中英混排不错位、不缺字） |
| E7 | 常见格式语法高亮（只高亮，不做任何语言服务） |

### 2.3 AI 面板（产品的另一半灵魂）

| 需求 | 说明 |
|------|------|
| A1 | 右侧 AI 面板，默认收起，一键展开/收起 |
| A2 | 五个操作按钮：**总结、分析、改写、问答、翻译**。无选中时作用于整个文件，有选中时作用于选中内容 |
| A3 | **改写、翻译：结果直接替换原文**，替换后 Ctrl+Z 一步撤销（已确认采用此方案） |
| A4 | 问答：自由提问，上下文自动携带当前文件内容（或选中内容） |
| A5 | 流式输出显示；请求可随时取消（Esc 或按钮）；请求失败友好提示并可重试 |
| A6 | 发送前显示预估 token/字符量；所有 AI 请求必须用户显式点击触发，**没有任何后台自动发送** |
| A7 | 本地模型支持：provider 列表内置「本地 Ollama / LM Studio」选项，敏感文件可不出机 |

### 2.4 配置（全产品唯一配置项）

- 只有 LLM Provider：下拉选择预设（OpenAI / DeepSeek / Kimi / 通义 / 本地 Ollama…），填 API Key 和模型名即可用。
- 首次点击任何 AI 按钮时弹出这个一次性配置框；配过一次不再打扰。
- 没有插件目录、没有用户脚本、没有 settings.json 暴露给用户、没有命令面板。

---

## 三、明确不做清单（V1 的减法承诺）

以下功能**明确排除**，防止范围蔓延（每一项都来自「不为核心链路服务」的判定）：

- ❌ 插件系统 / 插件市场 / 用户 Lua 脚本
- ❌ LSP / 代码补全 / 跳转定义 / 诊断
- ❌ 项目级搜索、代码折叠、多窗口、分屏
- ❌ 内置终端、git 集成
- ❌ Markdown 预览、写作模式、专注模式
- ❌ 会话恢复、工作区、自动保存（保留手动 Ctrl+S 传统）
- ❌ 设置 GUI（除 LLM provider 外无可配置项）
- ❌ 协作、账号、云同步、任何遥测

> 旧版《LiteXL改造路线图.md》中未被本文件覆盖的功能，降级为 **V2+ 备选 backlog**，不在 V1 讨论范围。

---

## 四、非功能需求（硬指标，CI 守门）

| 指标 | 目标 |
|------|------|
| 冷启动到可交互 | ≤ 300ms |
| 打开文件到可编辑 | ≤ 500ms |
| 内存占用（单文件编辑中） | ≤ 50MB |
| 安装包体积 | ≤ 20MB |
| 空闲 CPU | ≈ 0%（事件驱动，无轮询） |
| 离线可用性 | 未配置 LLM 或断网时，全部编辑功能 100% 可用 |
| 大文件 | ≥ 10MB 文件可打开编辑（可关闭高亮保流畅），不崩溃不卡死 |

---

## 五、关键设计决策记录

| 决策点 | 结论 | 理由 |
|--------|------|------|
| 多文件组织 | 简单多标签 | 兼顾对照编辑与极简，标签不持久化 |
| 文件夹打开 | 左侧文件树 | 已是用户心智模型，成本极低 |
| AI 改写落盘方式 | 直接替换 + 可撤销 | 选择「快」；撤销兜底足够安全 |
| AI 功能集 | 总结/分析/改写/问答/翻译 | 覆盖读、问、改、译四个意图，一个不多 |
| 状态策略 | 启动即空，仅存历史列表 | 「每次打开都是干净的」是产品气质的一部分 |

---

## 六、风险与对策

| 风险 | 对策 |
|------|------|
| 打开速度不达标（解码/字体/大文件） | 懒加载语法高亮；字体子集化；>10MB 自动关高亮；性能指标进 CI |
| GBK 编码误判 | 用成熟的编码检测启发式 + 状态栏明示当前编码，用户可手动切换 |
| AI 请求慢导致面板假死感 | 全异步 + 流式输出 + 取消按钮；按钮点击后立即给本地反馈 |
| AI 改写误伤文件 | 替换前内存中留快照，Ctrl+Z 可完整回滚；文件未保存不落盘 |
| LLM 供应商协议变动 | 只依赖 OpenAI 兼容协议；provider 预设可远程更新 |

---

## 七、验收标准（V1 完成定义）

1. 从下载安装到打开第一个文件并点出 AI 总结，全程不需要阅读任何文档、不需要输入任何命令。
2. 关闭软件再打开，界面为空，历史列表里有刚才的文件。
3. 断网状态下，除 AI 按钮外一切功能正常。
4. 整个产品只有一个配置框：LLM Provider。
